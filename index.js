// Access the global variable ReactDom and render something to the screen like an HTML
// Place it in the div with the id of root
// ReactDOM.render(content, position)
ReactDOM.render(<h1>Hello, Everyone!</h1>, document.getElementById('root'));
